# 2.3.3 双链表

prior 与 next 双向指针、插入删除边界和双向遍历

## 学习目标

- 理解双链表结点包含 prior、data、next 三部分。
- 掌握双链表初始化、判空、后插、删除和销毁的基本写法。
- 理解双链表可以向前遍历，也能更方便地找到前驱。

## 知识点结构

- **双链表结点结构：** 单链表只能通过 next 找后继，给定某个结点时找前驱很麻烦。双链表在每个结点中增加 prior 指针，指向前驱结点，同时保留 next 指向后继结点。
- **双链表插入：** 在结点 p 后插入 s，需要同时维护四条关系：s->next 指向 p 的原后继；若 p 原来有后继，则原后继的 prior 指向 s；s->prior 指向 ...
- **双链表删除和销毁：** 删除 p 的后继结点 q 时，先令 p->next = q->next；若 q 有后继，再令 q->next->prior = p；最后 free(q)。若 p...
- **双向遍历与查找：** 双链表可以沿 next 从前往后遍历，也可以沿 prior 从某个结点往前遍历。若不想处理头结点，向前遍历时可在 p->prior == NULL 前停止。

## 一、双链表结点结构

单链表只能通过 next 找后继，给定某个结点时找前驱很麻烦。双链表在每个结点中增加 prior 指针，指向前驱结点，同时保留 next 指向后继结点。

带头结点双链表初始化时，申请头结点，并把头结点的 prior 和 next 都设为 NULL。判空只需判断 L->next == NULL。

关键提示：
- DNode 中的 D 表示 double。

**本部分重点：** 双链表用额外的 prior 指针换取双向移动能力。


## 二、双链表插入

在结点 p 后插入 s，需要同时维护四条关系：s->next 指向 p 的原后继；若 p 原来有后继，则原后继的 prior 指向 s；s->prior 指向 p；p->next 指向 s。

若 p 是尾结点，p->next 为 NULL，此时不能访问 p->next->prior，应先判断 p->next 是否为空。指针赋值顺序也很重要，过早改 p->next 会丢失原后继。

关键提示：
- 先保存或使用原后继，再改 p->next。

**本部分重点：** 双链表插入要同时接好前后两个方向，尾结点情况要避免空指针。


## 三、双链表删除和销毁

删除 p 的后继结点 q 时，先令 p->next = q->next；若 q 有后继，再令 q->next->prior = p；最后 free(q)。若 p 本来没有后继，删除失败。

销毁双链表可以循环删除头结点的后继结点，直到表空，最后释放头结点并把头指针置空。由 malloc 申请的每个结点都应释放。

关键提示：
- 删除尾结点时要跳过 q->next->prior。

**本部分重点：** 删除操作既要断开 next，也要修复 prior，最后释放结点空间。


## 四、双向遍历与查找

双链表可以沿 next 从前往后遍历，也可以沿 prior 从某个结点往前遍历。若不想处理头结点，向前遍历时可在 p->prior == NULL 前停止。

双链表仍然不支持随机存取。按位查找、按值查找依旧需要遍历，时间复杂度通常为 O(n)。

关键提示：
- 双向不是随机存取。

**本部分重点：** 双链表比单链表更方便双向移动，但查找某个位序仍是 O(n)。

## 代码整理

### 双链表插入与删除

包含尾结点边界判断的后插和删除操作。

```c
#include <stdbool.h>
#include <stdlib.h>

typedef int ElemType;
typedef struct DNode {
    ElemType data;
    struct DNode *prior;
    struct DNode *next;
} DNode, *DLinkList;

bool InsertNextDNode(DNode *p, DNode *s) {
    if (p == NULL || s == NULL) {
        return false;
    }
    s->next = p->next;
    if (p->next != NULL) {
        p->next->prior = s;
    }
    s->prior = p;
    p->next = s;
    return true;
}

bool DeleteNextDNode(DNode *p) {
    if (p == NULL || p->next == NULL) {
        return false;
    }
    DNode *q = p->next;
    p->next = q->next;
    if (q->next != NULL) {
        q->next->prior = p;
    }
    free(q);
    return true;
}
```

## 易错点

- **删除时只改 next 不改 prior：** 双链表有两个方向的连接，必须保持一致。
- **尾结点插入或删除访问空指针：** p->next 或 q->next 为 NULL 时不能再访问其 prior。
- **认为双链表可随机存取：** 双链表能双向走，但仍需沿指针逐个查找。

## 本节总结

- 双链表结点包含 prior、data、next。
- 插入和删除要维护前后两个方向的指针。
- 边界处要检查后继是否为空，避免空指针访问。
- 双链表可双向遍历，但查找复杂度仍通常为 O(n)。
